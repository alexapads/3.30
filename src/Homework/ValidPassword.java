package Homework;

public class ValidPassword {
    public static void main(String[] args) {
       //boolean isThereUpperCase = !password.equals(password.toLowerCase());    //If password equals password.toLowerCase than there can't be an upperCase
        //boolean isThereLowerCase = !password.equals(password.toUpperCase());    //If password equals password.toUpperCase than there can't be a lowerCase
        //System.out.println(isThereUpperCase&&isThereLowerCase);
    }
}
